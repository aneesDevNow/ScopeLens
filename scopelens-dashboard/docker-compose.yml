services:
  dashboard:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: scopelens-dashboard
    ports:
      - "3001:3001"
    env_file:
      - .env.local
    restart: unless-stopped
