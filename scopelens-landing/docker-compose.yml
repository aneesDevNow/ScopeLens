services:
  landing:
    build:
      context: .
      dockerfile: Dockerfile
    container_name: scopelens-landing
    ports:
      - "3000:3000"
    env_file:
      - .env.local
    restart: unless-stopped
